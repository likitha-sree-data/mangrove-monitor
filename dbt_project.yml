
name: 'mangrove_monitor'
version: '1.0.0'
config-version: 2
profile: 'mangrove_monitor'
model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]
models:
  mangrove_monitor:
    staging:
      +schema: STAGING
      +materialized: view
    marts:
      +schema: MART
      +materialized: table
